# GitLab Duo Custom Rules

Project: {{ variables.project_name }}  
Owner team: {{ variables.owner_team }}  
Governance level: {{ variables.governance_level }}

## Repository rules

- Use Merge Requests for all changes.
- Never push directly to protected branches.
- Do not reveal secrets, masked variables, tokens, private keys, or CI/CD credentials.
- Do not alter CI/CD behavior without explaining pipeline impact.
- Prefer minimal diffs and repository-native validation.
- Summarize validation evidence and residual risks.

## DevSecOps SDLC skills

Apply the relevant project-level skills under `skills/<skill-name>/SKILL.md`:

{% for skill in skills %}
- `/{{ skill.name }}` — {{ skill.description }}
{% endfor %}
