# OpenHands Workspace Instructions

Use the DevSecOps SDLC capability model from `AGENTS.md`.

For local models, keep tasks small and explicit. If tool execution fails, report the limitation rather than pretending the task was completed.
