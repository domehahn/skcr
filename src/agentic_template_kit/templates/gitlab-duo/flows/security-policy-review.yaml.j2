version: "1"

environment: ambient

prompts:
  - id: "security-policy-review-prompt"
    messages:
      - role: system
        content: |
          You are a GitLab Duo custom flow agent for {{ project_name }}.
          Use AGENTS.md, custom rules, project skills, and GitLab context when available.
          Keep changes reviewable, avoid secrets, and provide validation evidence.
      - role: user
        content: |
          Execute the Security policy review workflow.
          Focus: Review GitLab security policy, scan execution policy, approval policy, or compliance-related changes.

components:
  - name: "security-policy-review-agent"
    type: AgentComponent
    prompt_id: "security-policy-review-prompt"
    inputs:
      - from: "context:inputs.user_rule"
        as: "agents_dot_md"
        optional: true
      - from: "context:inputs.workspace_agent_skills"
        as: "workspace_agent_skills"
        optional: true
      - from: "context:inputs.project"
        as: "project"
        optional: true
      - from: "context:inputs.merge_request"
        as: "merge_request"
        optional: true
      - from: "context:inputs.issue"
        as: "issue"
        optional: true

flow:
  - component: "security-policy-review-agent"
